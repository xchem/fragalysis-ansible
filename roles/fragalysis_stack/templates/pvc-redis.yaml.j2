---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: redis
  namespace: {{ stack_namespace }}
spec:
  accessModes:
  - ReadWriteOnce
{# A blank (whitespace-only) class omits the field so the cluster's #}
{# default StorageClass is used; setting it to '' would instead    #}
{# disable dynamic provisioning.                                    #}
{% if redis_vol_storageclass | trim | length > 0 %}
  storageClassName: '{{ redis_vol_storageclass | trim }}'
{% endif %}
  resources:
    requests:
      storage: '{{ redis_vol_size_g }}Gi'
